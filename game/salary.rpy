label salary:

    $ monthsalary += daysalary
    $ dayworksuccessfulhours = 0
    $ daysalary = 0
    if calendar.days == 1:
        $ money += monthsalary
        $ monthsalary = 0            
        $ monthworkresulthours = 0
        $ cooksalaryperhour = float(23.5*salarystatus)
    else:
        pass

    return